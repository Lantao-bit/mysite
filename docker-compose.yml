services:
  portfolio:
    build: .
    ports:
      - "8080:5000"
    environment:
      - SECRET_KEY=change-me-in-production
      - DATABASE_PATH=/app/data/portfolio.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
